<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:GHelper.Linux.UI.Controls"
        x:Class="GHelper.Linux.UI.Views.NoiseWindow"
        Title="Noise Suppression"
        Width="420"
        MinWidth="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackground}"
        Icon="avares://ghelper/UI/Assets/favicon.ico">
    <Border Classes="panel" Padding="14" Margin="10">
        <StackPanel Spacing="14">
            <StackPanel Orientation="Horizontal" Spacing="6"
                        VerticalAlignment="Center">
                <ctrl:Icon IconName="brain" Width="18" Height="18"
                           VerticalAlignment="Center" />
                <TextBlock x:Name="labelHeader"
                           Text="RNNoise" FontSize="16"
                           FontWeight="SemiBold" Foreground="{DynamicResource TextForeground}"
                           VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock x:Name="labelDescription"
                       Text="Neural-network voice/noise discrimination. The model is bundled and runs in the helper RT thread."
                       TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource TextDim}" />
            <StackPanel Spacing="4">
                <TextBlock x:Name="labelVadHeader" Text="Live voice activity" Foreground="{DynamicResource TextDim}" />
                <ProgressBar x:Name="meterVad" Minimum="0" Maximum="100" Height="14"
                             Foreground="{DynamicResource AccentColor}" Background="{DynamicResource ButtonBackground}" />
                <TextBlock x:Name="labelVad" Text="0%" Foreground="{DynamicResource AccentColor}" />
            </StackPanel>
            <StackPanel Spacing="4">
                <TextBlock x:Name="labelRedHeader" Text="Noise reduction" Foreground="{DynamicResource TextDim}" />
                <ProgressBar x:Name="meterRed" Minimum="0" Maximum="40" Height="14"
                             Foreground="{DynamicResource AccentHover}" Background="{DynamicResource ButtonBackground}" />
                <TextBlock x:Name="labelRed" Text="0 dB" Foreground="{DynamicResource AccentColor}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
